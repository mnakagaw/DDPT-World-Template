# Georgia independent audit and thematic comparison repair — 2026-09-26

**Country-edition verdict: REJECT; unpublished.** The audited partial candidate is `generated/georgia-areadata-20260926` (ignored local output), dataset SHA-256 `2f93be22179041b3f07cd86ba372ab92399b3774bf0a1ac725d1ba3437a33a75`. The independent checker reviewed fixed producer commit `dee7bbd45b8535293a2d2e65589ace928e9ff847` read-only, then rechecked the repair on pushed branch commit `eda8c7c353ce5ab4684564e1b91b88dfc0f50e4c`. The latter was integrated as `354af8b` on the Asia continuation branch. The checker and producer used separate worktrees; the checker did not edit the implementation or dataset.

## Independently verified partial scope

- All nine acquired originals matched their receipts and pinned hashes in the original and replay candidates. All 1,440 adopted Table 02/06 source cells were independently checked: 1,413 numeric values and 27 source-dash missing values, with zero mismatches. The candidate has 85 areas and zero polygons. The 63-column acquired-source register matched the inspected Excel files. The source-backed parent and field sums passed 207 independent checks; the 11 regional population rows sum to the directly reported country value 3,929,581.
- Seven saved diagnostic CSVs contained 720 domestic rows that matched dataset values, statuses, source URLs and parent relationships. Browser checks changed Georgia → Imereti → Kutaisi → Imereti without retaining Kutaisi documents or URL state. Didube had no district IDP value or parent clone. Kutaisi plan and original budget titles and the budget-edition caveat matched the acquired originals.
- On the first audited commit, a functional P1 was found: `analysis.comparisons` explicitly identified the 11 disjoint, source-backed regional reporting rows, and the territorial view showed 11/11, but the thematic view showed 0/11 because it rejected their differing legal types without reading the explicit cohort.

## Repair re-audit

The isolated repair permits mixed administrative types in a thematic comparison only when the selected cohort exactly matches an explicit membership list, has a nonempty membership explanation and acquired source IDs, and has a single boundary edition. The legacy implicit mixed-type and mixed-edition guards remain active. `npm run check` and `npm test` passed (219/219); both copied candidates were rebuilt and validated with zero errors/warnings. The primary dataset SHA stayed unchanged. In the producer and independent browser checks, Georgia and Imereti both showed 11/11 observed and ranked regions, median 303,833, range 29,901–1,331,485, and national member sum 3,929,581. The independent checker marked **P1 REPAIR ACCEPT**. The browser CSV action prepared an 11-row output; the independent check assembled the same header and 11 rows in memory, but did not inspect downloaded file bytes.

**Remaining acceptance blockers:** the 2024 official code and dated polygon crosswalk is absent; only part of the 2024 census and agriculture catalogue is audited; other municipalities' plans, current budget amendments, implementation and official evaluations are not collected; applicable 42 scenarios, narrow-screen and full-table print checks are not complete. These are country acceptance gaps, so the country edition remains **REJECT** and has no Hosting/Public release.
