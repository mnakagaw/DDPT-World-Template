# 世界版ダッシュボードの作業規則

このプロジェクトはDDPTを基準に、国別条件に対応する地域情報・計画策定ダッシュボードを作る。最初にREADME.mdと今回の依頼を読む。

## 国名だけで始める案件

- 「○○国版を作って」「このリポジトリを基に○○版」の依頼は実行依頼。`START_HERE.md`、`docs/COUNTRY_AGENT_WORKFLOW.md`を読み、国名の解決、初期収集、国内公式資料の収集・統合、サイト作成、検証まで進める。
- 国名だけを十分な開始情報として扱い、開始シートとTask ContractはAIが埋める。曖昧な国の同定など、結果を変える不明点以外で質問票を返さない。
- `create-country.mjs`は全国統計と参照境界の初期生成。実行成功だけで国別版完成とせず、公式の地方統計・コード・計画・予算を調査し、取得できる優先資料を組み込む。
- 取得不能・対象外・未公開・未照合は根拠と次の行動を記録する。原資料不足時も動作する代替表示と出力を完成させ、未完成の業務を明示する。
- 新しい国の成果は別ディレクトリへ生成する。テンプレート本体をその国用に書き換えない。中断・再開点は案件のHANDOFF.mdへ残す。
- 参照実装0.4（データschemaは0.2）とUX仕様v1.0の最終採用を区別する。v1.0未確定を理由に、許可された国別試作・調査・検証を停止しない。

## 世界・大陸・広域から始める案件

- 世界入口の依頼は`docs/WORLD_ADAPTER.md`と`docs/IMPLEMENTATION_CONTRACT.md`を読み、`node scripts/create-world.mjs --out <new-directory>`で取得・検証・生成する。既存出力は拒否し、原本からの再実行は`--source-dir <previous-project/raw>`を使う。国別版の作成に世界全体の収集を前提としない。
- UN M49の5 Regionと全country/area台帳、元の所属・例外・取得時点を保持する。独自Central America + CaribbeanはUN 013＋029の結合（Mexicoを含む）と明示し、UN/WB/OWIDの既存集計と同一視しない。探索区分へ法定計画義務を付与しない。
- 世界の公表値と、各国・国内地域の値を分ける。大陸・広域の同一範囲公式値がなければ欠測とし、国の合計・率平均で作らない。国際指標と国内統計の定義・単位・母集団・測定方法を自動同一化しない。
- 国内行政階層は別の国版datasetで接続する。接続する国・対象ID・安全なURL・指標の意味を確認し、対応未確認の指標を無言で引き継がない。国内統計・計画資料はその国のアダプターで収集する。
- 参照図形の未結合・小島省略を台帳からの削除理由にしない。上位位置図用の国ポリゴン連結は同じsource・版の表示専用とし、欠けた構成地域を記録する。法定境界・面積・統計値の集計には使わない。
- 既存のラテンアメリカ20か国調査と国勢調査・計画制度の方法を保持する。世界系列の取得やレポートの追加を、20か国の国内数値収集アダプター完成と扱わない。

## 仕様の扱い

- `docs/02_COMMON_SPEC.md`のC01～C07を共通の操作契約として扱う。最新版の明示指示を適用し、共通仕様全体のv1.0採用完了とは区別する。
- DDPTの地域診断とテーマ比較の目的・情報配置・連動を維持する。色や見出しだけ似せて、操作の手順を独自に作り直さない。
- 通常の実装判断、合意したデータ適応規則、意味を変えない不具合修正は自律的に進める。
- ページ統合、主要操作の移動・追加、比較の意味、指標削除、認証条件、保存互換性を変える場合、既存の指示にその根拠があるか確認する。範囲外なら具体的な差分案を作り、該当変更のみ判断を求める。独立作業は進める。
- 「データが少ない」「見た目がよい」は、ページの目的や選択時の挙動を変える理由にならない。`docs/03_COUNTRY_AND_DATA.md`の代替規則を使う。
- DDPT参照リポジトリ、他国の作業中リポジトリ、添付参考資料は読み取り対象。別の明示的な改修依頼なしに変更しない。

## データと検証

- 国・地域型・公式コード・境界版で対象を特定する。同名地域を統合しない。行政階層、統計分類、支援対象を分ける。
- 欠測・ゼロ・非該当・未確認・取得失敗を区別する。全国値を地方値として埋めない。未取得資料から計画不存在や未承認を断定しない。
- 地域選択は地図、見出し、全指標、資料、出力を一致させる。別のズーム操作を更新の前提にしない。
- 上部の分析対象を選ぶ位置図・階層選択・基本情報を保持し、内部比較地図・全件表は各指標の下部に置く。内部比較の地図・行への注目で上部の対象・他指標・資料・URL・出力先を変更しない。
- 地理階層では最後に明示選択した地域を分析対象にする。市・県を選択後、同じ所属先の上位地域を選び直す操作でも下位の選択を解除し、上位全体へ即時に切り替える。「所属先の表示」と「上位全体を選ぶ操作」を区別し、同じoptionの再選択でchangeイベントが起きない罠を避ける。指標・期間と地域別メモは保持する。
- 親の選択と、その子の一覧を絞る操作を混同しない。親を分析対象にした際に、以前の市の値・図・文書が残ることを回帰試験で防ぐ。親の値が未取得なら親自身の欠測を示し、子の値の残留や無根拠な再集計で埋めない。
- 内部比較の全体台帳・粒度・指標・同年・定義・母集団・単位・境界版を確認する。意味の違う元値は理由とともに残し、共通の色・数値比較から除く。基礎自治体の確認済み分類と任意`analysis.terminal_territory_ids`で細分停止を扱い、小地域があるだけで比較を追加しない。
- 計画・資料は選択地域の公式資料の確認と策定・見直しの基礎資料取得を目的にする。統合公式システム、分散資料、部分取得を情報源ごとに調べ、取得工程の件数より内容・期間・根拠を先に表示する。`docs/PLANNING_DATA_CONTRACT.md`に従い、取得段階と制度状態、予算執行・計画達成・公式評価を分ける。制度名・周期・章立て・出力形式を一国から全世界へ固定しない。
- 設計用`templates/country-profile.json`を実行設定と誤認しない。採用した設定は任意の`dataset.planning`へ明示し、旧データを再生成して互換性を検証する。更新失敗で最後の正常なサイトを置き換えず、停止状態を表示する。
- 分析の採用設定は任意の`dataset.analysis`へ明示する。実行版0.4でもdataset schema 0.2を保ち、任意拡張なしの旧dataset・URL・統計CSV・計画資料出力を回帰確認する。国別メモや保存機能を内部比較の追加に便乗して変更しない。
- 画面、保存内容、Word/PDF/CSVの地域・期間・単位・出典を照合する。取得成功だけで内容一致を合格にしない。
- 検証記録に対象commit・データ版・環境・日付・未実施を記す。過去の合格件数を新しい版へ流用しない。
- Host Microsoft Word COMを使わない。文書の検証は隔離された文書レンダラーで行う。

## 作業範囲と公開

- 無関係な変更を含む作業ツリーでstage、revert、上書きをしない。必要に応じて独立した作業場所を使う。
- 新しい国の保存先・公開先・提供範囲を開始シートで特定する。既存案件のパス、資格情報、DDPTの公開先を他国へ自動転用しない。
- 公開依頼と運用条件が整っている作業は、限定した成果物をcommit・push・検証し、その版の公開まで完了する。許可済みの工程のために繰り返し確認しない。
- テンプレート本体の納品はPrivate GitHubへのコード・仕様・検証記録の保存。国別サイトの公開先は案件の依頼範囲に従う。
- 標準検証は`npm run check`、`npm test`。国別データは`node scripts/validate-country.mjs --project <directory>`で検証し、実際の画面・取得物も確認する。
- `templates/ACCEPTANCE.md`の42シナリオを採用範囲に応じて確認し、実施・対象外・未実施を分ける。内部比較を含む診断出力は初行・末行・件数・出典を照合し、検索やスクロールで隠れた行を含む全表印刷を確認する。版0.4の結果は`docs/VALIDATION_v0.4.md`へ記録し、42シナリオの存在を42件合格と報告しない。
- 完了報告は成果と検証範囲を先に述べ、Local／GitHub／Hosting／Publicを必要に応じて分ける。
